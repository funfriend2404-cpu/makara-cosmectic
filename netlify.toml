[build]
  publish = "."
  functions = "netlify/functions"
  command = "echo 'No build step needed — installing dependencies only.'"

[functions]
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
